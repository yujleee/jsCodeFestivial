'use strict';

//문제 41: 소수 판별 프로그램
//소수면 yes, 아니면 no를 출력
{
  const num = parseInt(prompt('숫자를 입력하세요'));

  if (num % num === 0) {
    console.log('No');
  } else if (Math.sqrt(i)) {
    console.log('YES');
  }
}
